version: "3.8"
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: elt_db
      POSTGRES_USER: elt_user
      POSTGRES_PASSWORD: elt_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
